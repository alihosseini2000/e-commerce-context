import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'imgur.com',
      'i.imgur.com',
      'api.escuelajs.co',
      'placeimg.com',
      'placehold.co',
      'm.media-amazon.com',
      'img.freepik.com',
    ]
  }
};

export default nextConfig;
